# shellcheck shell=bash

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [ -z "$WAYLAND_DISPLAY" ] && [ -n "$XDG_VTNR" ] && [ "$XDG_VTNR" -eq 1 ] ; then
  ~/Scripts/with_colors.sh ~/.config/sway/config.with_colors
  exec sway
fi
